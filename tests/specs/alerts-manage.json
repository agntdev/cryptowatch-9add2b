[
  {
    "name": "Manage Alerts shows watchlist for alert setup",
    "steps": [
      {
        "send": { "callback": "alerts:manage" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Your watchlist is empty — add coins first to set alerts."
            }
          }
        ]
      }
    ]
  }
]
